3;                      // number of connectivity layers
2-100-5-3;          // network configuration
true;                   // true if training, false if running
-1.5;                    // lowest value for random generation
1.5;                    // highest value for random generation
0.3;                    // lambda (learning rate)
100000;                 // maximum number of training iterations
0;                  // keep alive interval
0.0002;                   // average error threshold (stop when average error <= this value)
false;                  // option to show the input table
false;                   // option to show the truth table
4;                      // number of cases
RANDOM;                 // method of choosing weights (LOAD, MANUAL, or RANDOM)
false;                   // option to save the weights
trainedWeights.bin;     // name of file to load weights from
trainedWeights.bin;     // name of file to save weights to
truthTable.txt;    // name of file to load truth table from
inputTable.txt;           // name of folder to load input table from
FILE;    // method of choosing input table (FILE or FOLDER)